/* 
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
/*$(document).ready(function(){
 $(document).on('click', '.btnmodal ', function(){
 var id=$('#txtidcl').text();
 var nombre=$('#txtnombrecl').text();
 var apaterno=$('#txtapaternocl').text();
 var amaterno=$('#txtamaternocl').text();
 var direccion=$('#txtdireccioncl').text();
 var celular=$('#txtcelularcl').text();
 var estado=$('#txtestadocl').text();
  alert(nombre)
 $('#examplemodal').modal('show');
 $('.modal-body #txtid').val(id);
 $('.modal-body #txtnombre').val(nombre);
 $('.modal-body #txtapaterno').val(apaterno);
 $('.modal-body #txtamaterno').val(amaterno);
 $('.modal-body #txtdireccion').val(direccion);
 $('.modal-body #txtcelular').val(celular);
 $('.modal-body #txtestado').val(estado);
 });
 });*/

/*$('#dataUpdate').on('show.bs.modal', function (event) {
    var button = $(event.relatedTarget); // Botón que activó el modal
    var codigo = button.data('codigo'); // Extraer la información de atributos de datos
    var id = button.data('id'); // Extraer la información de atributos de datos
    var nombre = button.data('nombre'); // Extraer la información de atributos de datos
    var moneda = button.data('moneda'); // Extraer la información de atributos de datos
    var capital = button.data('capital'); // Extraer la información de atributos de datos
    var continente = button.data('continente'); // Extraer la información de atributos de datos

    var modal = $(this);    
    modal.find('.modal-body #id').val(id);
    modal.find('.modal-body #codigo').val(codigo);
    modal.find('.modal-body #nombre').val(nombre);
    modal.find('.modal-body #moneda').val(moneda);
    modal.find('.modal-body #capital').val(capital);
    modal.find('.modal-body #continente').val(continente);
    $('.alert').hide();//Oculto alert
})

$('#dataDelete').on('show.bs.modal', function (event) {
    var button = $(event.relatedTarget) // Botón que activó el modal
    var id = button.data('id') // Extraer la información de atributos de datos
    var modal = $(this)
    modal.find('#id_pais').val(id)
});*/
